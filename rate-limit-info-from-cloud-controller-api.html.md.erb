---
title: Rate Limit Information Returned by the Cloud Controller API
owner: CAPI
---

The Cloud Controller API includes rate limit information in the HTTP header. Each header includes the following:

```
X-RateLimit-Limit: 60
X-RateLimit-Remaining: 56
X-RateLimit-Reset: 1372700873
```

Use this table to understand the rate limit header.

<table>
<th>Field Value</th>
<th>Description</th>
 <tr>
 	<td>X-RateLimit-Limit</td>
 	<td>The maximum number of attempts per IP address.</td>
 </tr>
 <tr>
 	<td>X-RateLimit-Remaining</td>
 	<td>The number of attempts remaining. Customize this value with <pre>cc.rate_limiter.reset_interval_in_minutes</pre>.</td>
 </tr>
 <tr>
 	<td>X-RateLimit-Reset</td>
 	<td>The time at which the rate limit resets, in UTC <a href="https://en.wikipedia.org/wiki/Unix_time">epoch seconds</a>. Customize this value with <pre>cc.rate_limiter.reset_interval_in_minutes</pre>.</td>
 </tr>
</table>

When requests exceed the rate limit value, the Cloud Controller API returns a `429: Too Many Requests` error code.

For more information about setting custom rate limits, see the [Setting the Rate Limit for the Cloud Controller API](./setting-rate-limit-cloud-api.html) topic.